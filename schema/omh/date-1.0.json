{
    "$schema": "http://json-schema.org/draft-04/schema#",

    "description": "This schema represents a full or partial date (for example: 2014-11-20, 2016-09, 2011). See RFC 3339 5.6 for details.",
    "type": "string",

    "references": [
        {
            "description": "The SNOMED code represents Date (qualifier value).",
            "url": "http://purl.bioontology.org/ontology/SNOMEDCT/410672004"
        }
    ],

    "oneOf": [
        {
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"

        },
        {
            "pattern": "^[0-9]{4}-[0-9]{2}$"
        },
        {
            "pattern": "^[0-9]{4}$"
        }
    ]
}
